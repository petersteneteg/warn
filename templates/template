#if !defined({prefix}WARN_INCLUDE_PUSH)
#   error "`{path}/{ignore}/{name}` used without `{path}/push`"
#endif

#if defined({prefix}{guard})
#   error "`{path}/{ignore}/{name}` already included"
#endif

#define {prefix}{guard}

{contents}